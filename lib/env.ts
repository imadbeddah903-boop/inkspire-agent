function req(name:string){const v=process.env[name];if(!v)throw new Error(`Missing environment variable: ${name}`);return v}
export const env={supabaseUrl:req("NEXT_PUBLIC_SUPABASE_URL"),supabaseAnonKey:req("NEXT_PUBLIC_SUPABASE_ANON_KEY"),supabaseServiceRoleKey:req("SUPABASE_SERVICE_ROLE_KEY"),deepseekApiKey:req("DEEPSEEK_API_KEY"),githubAppId:req("GITHUB_APP_ID"),githubPrivateKey:req("GITHUB_APP_PRIVATE_KEY").replace(/\\n/g,"\n"),githubInstallationId:req("GITHUB_INSTALLATION_ID"),githubOwner:req("GITHUB_OWNER"),githubRepo:req("GITHUB_REPO"),upstashUrl:req("UPSTASH_REDIS_REST_URL"),upstashToken:req("UPSTASH_REDIS_REST_TOKEN")};