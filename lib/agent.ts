import {generateText} from "ai";import {createOpenAI} from "@ai-sdk/openai";import {systemPrompt} from "./prompts";import {checkRateLimit} from "./ratelimit";const deepseek=createOpenAI({apiKey:process.env.DEEPSEEK_API_KEY,baseURL:"https://api.deepseek.com"});export async function runAgent(message:string){await checkRateLimit("chat");const result=await generateText({model:deepseek("deepseek-chat"),system:systemPrompt,prompt:message});return {text:result.text,toolsAvailable:["repo","projects"]};}