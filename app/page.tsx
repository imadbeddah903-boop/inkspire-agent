"use client";
import {useState} from "react";
export default function Page(){const [message,setMessage]=useState("");const [reply,setReply]=useState("");const [busy,setBusy]=useState(false);async function send(){if(!message.trim())return;setBusy(true);setReply("");const r=await fetch("/api/chat",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({message})});const j=await r.json();setReply(j.text||j.error||"No response");setBusy(false);}return <main><h1>InkSpire Agent</h1><p>Read-only intelligence with approval-gated GitHub changes.</p><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Ask about InkSpire..." /><button onClick={send} disabled={busy}>{busy?"Working…":"Send"}</button>{reply&&<pre>{reply}</pre>}</main>}