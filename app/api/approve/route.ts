import {NextResponse} from "next/server";import {setApproval} from "@/lib/upstash";
export async function POST(req:Request){try{const body=await req.json();if(!body?.approvalId||typeof body.approved!=="boolean")return NextResponse.json({error:"approvalId and boolean approved are required"},{status:400});await setApproval(String(body.approvalId),body.approved);return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"request failed"},{status:500});}}